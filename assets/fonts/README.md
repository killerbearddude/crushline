# Fonts

Place project fonts here.

The renderer first looks for `assets/fonts/Inter-Regular.ttf`, then falls back to common
system fonts such as DejaVu Sans, Liberation Sans, and Noto Sans. Font files are not
checked into this repository yet.
